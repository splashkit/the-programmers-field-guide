---
title: Variable Expression
---
import { YouTube } from 'astro-embed';

With the variable in place, let's see how to use the variable within expressions.

Watch this video to:

- Use the value of a variable.

That covers the main uses for variables, so next we move on to constants.
